# fzhang.dev

这是一个基于 Hugo 与 `ananke` 主题构建的个人网站。

## 常用命令

- 本地预览：`hugo server -D`
- 生产构建：`hugo --environment production --minify`

## 发布约定

- 文章统一存放在 `content/posts/`
- 站点配置集中在 `hugo.toml`
- `public/` 为构建产物，不再纳入版本控制
